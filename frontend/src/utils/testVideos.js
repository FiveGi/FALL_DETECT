/**
 * ป้ายกำกับของไฟล์วิดีโอทดสอบในดรอปดาวน์ "เลือกไฟล์วิดีโอทดสอบ"
 *
 * ดรอปดาวน์นี้มีอยู่สองที่ (ฟอร์มเพิ่มกล้องใน CameraManagementView และ CameraEditModal)
 * จึงเก็บวิธีเขียนป้ายไว้ที่เดียว ไม่ต้องแก้สองแห่งทุกครั้ง
 *
 * คำอธิบายมาจาก backend (`Test/clips.json`) ซึ่งเป็นไฟล์เสริม ถ้าไม่มีก็แสดงแค่ชื่อไฟล์
 * เหมือนเดิม เหตุผลที่ต้องมี: `17.mp4` ไม่มีการล้มอยู่ในคลิปเลย การที่ระบบเงียบคือคำตอบ
 * ที่ถูกต้อง แต่ถ้าเห็นแค่เลข "17.mp4" คนที่เพิ่งเปิดใช้จะคิดว่าระบบพัง
 */
export function testVideoLabel(video) {
  if (!video) return ''
  return video.description ? `${video.filename} — ${video.description}` : video.filename
}
