Flask
Flask-SQLAlchemy
Flask-JWT-Extended
psycopg2-binary
mysql-connector-python
celery
flower
redis
python-dotenv
requests
opencv-python
Pillow
numpy<2.0.0
scikit-image
onnxruntime
Flask-CORS
mediapipe
pytz
ultralytics
# Ships a static ffmpeg with libx264. opencv-python's bundled ffmpeg has no H.264
# ENCODER (licensing), so cv2.VideoWriter silently falls back to mp4v, which LINE
# and iOS refuse to play -- and the alert clip exists to be watched on a phone.
imageio-ffmpeg
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124
tqdm
rfdetr